{
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "test": "jest",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --loader:.png=dataurl --loader:.svg=text --target=chrome58,firefox57,safari11,edge16 --outdir=build",
    "watch": "esbuild src/index.ts --bundle --loader:.png=dataurl --loader:.svg=text --outdir=build --watch",
    "stylelint": "npx stylelint **/*.css",
    "prettier": "npx prettier --write src/",
    "prettier:check": "npx prettier --check"
  },
  "lint-staged": {
    "**/*.css": ["stylelint", "prettier --write --ignore-unknown"],
    "**/*.scss": ["stylelint --syntax=scss", "prettier --write --ignore-unknown"],
    "**/*.ts?(x)": ["prettier --write --ignore-unknown"]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "chokidar": "^3.5.1",
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "lint-staged": "^11.0.0",
    "prettier": "2.3.0",
    "stylelint": "^13.13.1",
    "stylelint-config-recommended": "^5.0.0",
    "stylelint-config-standard": "^22.0.0"
  },
  "dependencies": {
    "esbuild": "^0.11.20"
  }
}
